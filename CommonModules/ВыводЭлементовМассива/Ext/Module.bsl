Процедура ВыводЭлементовМассива() Экспорт
		
		Массив = Новый Массив;

	Массив.Добавить (4);
	Массив.Добавить (5);
	Массив.Добавить (5);
	Массив.Добавить (26);
	Массив.Добавить (4);

	
	Сообщить("--Массив:--");
	
	Для К=0 По 4 Цикл
		   Сообщить (Массив[К]);
		   
		   Если Массив [К] = 26 Тогда 
			   Сообщить ("26 – это слишком много")
			   КонецЕсли;
	КонецЦикла
	
	  	  
		
КонецПроцедуры
